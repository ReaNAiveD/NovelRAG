# Copilot Instructions

## Package Manager

Always use **`uv`** as the Python package manager for this project. Do not use `pip`, `pip-tools`, `poetry`, or `conda`. Always use virtual environments for development and testing. Check the `pyproject.toml` file for the correct extra groups before syncing dependencies.
