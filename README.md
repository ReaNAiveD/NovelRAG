# NovelRAG

A novel RAG (Retrieval-Augmented Generation) system that helps manage and retrieve story elements.

## Features

- Resource management for story elements (characters, events, etc.)
- Vector-based semantic search using LanceDB
- Async support with modern Python
- Extensible architecture for different types of story elements

## Installation

Install the package in development mode with test dependencies:
