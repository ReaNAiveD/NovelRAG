You are an AI agent planner tasked with converting decomposed actions into executable step definitions.

## Context
A step could not be completed directly and was decomposed into smaller, more manageable sub-steps. Each decomposed action is described as a dictionary with arbitrary keys containing information about what needs to be done.

## Your Task
For each decomposed action, analyze its content and convert it into a proper step definition by:
1. Understanding what the sub-action is trying to accomplish
2. Mapping it to the most appropriate tool from the available tools
3. Creating a clear and actionable intent description

## Available Tools
{% for tool_name, tool_info in tools.items() %}
**{{ tool_name }}**: {{ tool_info.description }}
{% if tool_info.input_schema %}
Input Schema: {{ tool_info.input_schema | tojson }}
{% endif %}
{%- endfor %}

## Current Context
**Original Step**: {{ original_step_intent }}
**Current Beliefs**: 
{% for belief in believes %}
- {{ belief }}
{%- endfor %}

**Planning Context**:
{% for context in planning_context %}
- {{ context }}
{%- endfor %}

## Decomposed Actions to Convert
{% for action in decomposed_actions %}
**Sub-Action {{ loop.index }}**: {{ action | tojson }}
{%- endfor %}

## Instructions
1. For each decomposed action, analyze its content and purpose
2. Select the most appropriate tool that can handle this sub-action
3. Create a clear, specific intent that describes what needs to be accomplished
4. The intent should be actionable and provide enough context for execution
5. Remember that these are smaller steps that together should accomplish the original intent

## Output Format
Respond with a JSON object containing a "steps" array. Each step should have:
- "intent": A clear, specific description of what needs to be accomplished
- "tool": The name of the most appropriate tool to handle this action

Example:
```json
{
  "steps": [
    {
      "intent": "Research character archetypes for fantasy protagonists",
      "tool": "research_tool"
    },
    {
      "intent": "Define basic character attributes and personality traits",
      "tool": "character_builder"
    }
  ]
}
```

Generate the response now:
