# 基于当前目标进行规划
# 拥有一个工具集，需要从中选择工具
# 需要考虑依赖项的创建，如果当前意图存在待完成目标作为依赖，则挂起当前规划，并推入新的目标
# 需要考虑为完成当前目标的链路中，已经完成的任务
# 工具一般包括
    # 基于内容查询筛选
    # 设置当前上下文
    # 各类型资源的增删改
    # 创建新的资源类型以及意图工具
    # 知识查询
    # 推入新的目标
    # 进行关联更新
    # 定义的 MCP
# 如果此前操作为读，那边需要判断当前信息是否足够，是否需要追加其他的读操作